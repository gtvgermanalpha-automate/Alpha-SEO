/**
 * Intentionally empty. The Contemporary Private Equity direction uses a clean,
 * pure-white background with no decorative shapes, gradients or patterns.
 * Kept as a no-op so any stray import stays harmless.
 */
export function FloatingShapes() {
  return null;
}
